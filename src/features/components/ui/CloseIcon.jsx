import React from 'react';
import { X } from '@phosphor-icons/react';

export default function CloseIcon(props) {
  return (
    <X
      weight="regular"
      aria-hidden="true"
      {...props}
    />
  );
}
